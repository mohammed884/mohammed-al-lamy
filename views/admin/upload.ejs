<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../styles/style.css">
    <!-- <link rel="stylesheet" href="styles/index.css"> -->
    <style>
        @font-face {
            font-family: "cairo";
            src: url("../fonts/static/Cairo-Regular.ttf");
            font-display: swap;
        }

        * {
            font-family: "cairo";
        }
    </style>
    <title>Document</title>
</head>

<body style="background: #fdfdfd;">
    <div class="w-[100%] h-[100vh] flex items-center justify-center" style="direction: rtl;">
        <div class="sm:w-[90%] lg:w-[72%] 2xl:w-[50%] sm:h-[82vh] md:h-[70vh] bg-white flex flex-col items-center justify-center rounded-md relative"
            style="box-shadow: 4px 3px 0px 2px #EEF1FF;">
            <div class="sm:w-[90%] lg:w-[72%] 2xl:w-[50%] flex items-center justify-between mb-3">
                <div class="sm:w-[26%] sm:text-[1.4em] md:text-[1.7em] h-[3px] bg-blue-400 rounded-lg"></div>
                <h1 class="text-custom_dark sm:text-[1.6em] md:text-[1.7em] font-bold">هلا بيك</h1>
                <div class="sm:w-[26%] sm:text-[1.4em] md:text-[1.7em] h-[3px] bg-blue-400 rounded-lg "></div>
            </div>
            <form action="/admin/upload" method="POST" enctype="multipart/form-data"
                class="sm:w-[90%] lg:w-[70%] 2xl:w-[50%]  flex flex-col">
                <% if (messages.danger) { %>
                    <span class="w-[100%] bg-red-100  mt-3 rounded-sm border-r-2 border-red-600 p-2">
                        <%=messages.danger%>
                    </span>
                    <%}%>
                    <% if (messages.success) { %>
                        <span class="w-[100%] bg-green-100  mt-3 rounded-sm border-r-2 border-green-600 p-2">
                            <%=messages.success%>
                        </span>
                        <%}%>
                        <label for="file" id="file-input-label"
                            class="w-[100%] h-[47px] flex items-center  border-b border-b-light_green mt-5 rounded-[5px] p-2 cursor-pointer">
                            <span>اختر فايل</span>
                            <div class="w-7 h-7 mr-2">
                                <img src="/fileUpload.svg" alt="file upload icon">
                            </div>
                        </label>
                        <input id="file-input" type="file" name="file" class="hidden" type="text"
                            placeholder="اكتب رمز الحماية" />
                        <button class="text-[#181818] bg-blue-500 text-custom_dark ml-auto mt-5 rounded-md p-2">تحميل
                            الفايل</button>
            </form>
            <%- include("../layout/footer.ejs")%>
        </div>
    </div>
    <script>
        const fileInput = document.querySelector("#file-input")
        const fileInputLabel = document.querySelector("#file-input-label");
        fileInputLabel.onclick = () => fileInput.click();
    </script>
</body>

</html>